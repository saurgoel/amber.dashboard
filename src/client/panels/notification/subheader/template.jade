mixin Tab(text, href, is_active)
	li.tab.col.s4(class="#{locals.tab} " + is_active ? "active" : "")
		a(href=href) #{text}

ul.tabs(class=locals.tabs)
	+Tab('Emails', '/notification/emails')
	+Tab('SMS', '/notification/sms')
	+Tab('Push', '/notification/push')