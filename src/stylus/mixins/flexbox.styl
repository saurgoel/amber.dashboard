flex-vertical()
	display flex
	flex-direction column

flex-horizontal()
	display flex
	flex-direction row
	
flex-container(direction = row, align_items = 0)
	display flex
	flex-direction direction
	
	if align_items
		align-items align_items